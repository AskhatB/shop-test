import { FC, ReactElement } from "react";
import { StyledTopControlsWrapper } from "./styles";

const TopControls: FC = (): ReactElement => {
  return <StyledTopControlsWrapper>wefwef</StyledTopControlsWrapper>;
};

export default TopControls;
